function modifyAverage(input) {
    let num = Number(input);
    function findNumberLength(num) {
        let a = -1;
        let b = -1;
        let c = -1;
        let d = -1;
        let e = -1;
        let f = -1;
        if (num / 100000 > 0) {
            a = num / 100000;
            b = num / 10000;
            c = num / 1000;
            d = num / 100;

        }
    }
    findNumberLength(num);
}
modifyAverage([101]);